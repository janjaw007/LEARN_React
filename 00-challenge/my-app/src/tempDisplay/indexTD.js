import react from "react";
import ReactDOM from "react-dom/client";
import App from "./AppTD";
const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <react.StrictMode>
    <App />
  </react.StrictMode>
);
